# Shared Layer

Contains cross-cutting, reusable primitives shared across modules.

Examples:
- Small pure utilities
- Shared contracts and constants
- Common value helpers
- Utility primitives consumed by domain/platform without legacy `utils` dependencies
